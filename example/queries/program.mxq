#!/usr/bin/env mxml

/**
 * A simple query
 * @param {?String} argv1 - The first argument to the query program
 */

import './variables'
import $coalesce($argv1, $MRSS_FEED_URL)

let nodes_contains_aws_domain = **[is node and text contains $AWS_DOMAIN]

$nodes_contains_aws_domain
